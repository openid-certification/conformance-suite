/**
 * AUTO-GENERATED — DO NOT EDIT BY HAND.
 *
 * Regenerate with:
 *   cd frontend && npm run codegen        (online; needs dev Spring server)
 *   cd frontend && npm run codegen:offline (re-merge from committed cache)
 *
 * Source: frontend/src/api/openapi.json (springdoc snapshot)
 *         frontend/src/api/samples/*.json (captured wire payloads)
 *
 * See frontend/src/api/README.md for the full workflow and read-only invariant.
 *
 * BOOTSTRAP STATE: this file is a placeholder until a contributor runs
 * `npm run codegen` against a running dev Spring server. Until then, every
 * `paths[...]` entry resolves to `unknown` and the JSDoc-typed consumers fall
 * back to `unknown` field access — which still type-checks under
 * `noImplicitAny: false`. Replace this file by running codegen.
 */
/* eslint-disable */

// Placeholder shape: every `paths[...]` access resolves to `any` until codegen
// runs and replaces this file with springdoc-derived path types and
// quicktype-derived field-level types. The `any` choice is deliberate: it lets
// JSDoc consumers compile against the real path string they expect to use, so
// the annotation site does not need to change after the first codegen run.
// Type safety arrives when codegen lands.
export interface paths {
  [path: string]: any;
}
