---
title: "MR 1998 maintainer-feedback thread tracker"
type: tracker
status: active
created: 2026-05-22
plan: docs/plans/2026-05-22-002-fix-mr1998-maintainer-feedback-plan.md
mr: https://gitlab.com/openid/conformance-suite/-/merge_requests/1998
---

# MR 1998 — maintainer-thread tracker

State of every review thread on [MR 1998](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998) versus the unit-by-unit execution of `docs/plans/2026-05-22-002-fix-mr1998-maintainer-feedback-plan.md`.

**Update protocol** (from `executing-plan-unit-by-unit` skill): before invoking `compound-engineering:ce-commit` for any unit, this table must reflect the change. Stage this file alongside the unit's code files so the commit captures both.

**Status values:**

- `open` — no commit yet addresses the thread.
- `partial` — at least one commit closes part of the thread; sub-issues remain. Reply on the thread but **leave it unresolved** on the MR until the rest lands.
- `closed` — every concern in the thread is addressed. Reply and mark resolved on the MR.
- `gated` — blocked on a Decision Gate (G1, G2, G3). No code-side work possible until the gate resolves.

---

## Threads

| Status | Finding | Author | Thread | Closing commit(s) | Suggested reply |
|---|---|---|---|---|---|
| closed | A5 (spinner half) | thomasdarimont | [#note_3371494631](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371494631) | `c63c21ea7` | "Fixed in c63c21ea7 — added a static `info` cts-badge variant and re-pointed cts-log-entry's INFO and HTTP request/response/incoming/outgoing markers to it. Verified in-situ on a real 27-row log: no `.cts-badge-spin` elements remain on any blue pill." *Note: this thread also contains the progress-bar (A4) concern. Reply scoped to "indicators stay animated"; leave thread unresolved until A4/U5 ships.* |
| closed | A5 + #21 | almgren | [#note_3371763018](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371763018) | `c63c21ea7` | "Fixed in c63c21ea7 — see Thomas's thread for details. The blue status pills now route through a static `info` variant; the spinner only fires for the genuinely-running state. The 'misalignment' subjective impression may have been the spinner's perpetual motion catching the eye — please reopen if you still see geometric drift after pulling." |
| closed | A6 | almgren | [#note_3371749447](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371749447) | `c63c21ea7` (spinner), `dc7e053a3` (WAITING copy + Start visibility) | "The remaining half of this thread is fixed in `dc7e053a3` (U3). The WAITING-state copy now distinguishes two cases — when no conditions have run yet the bar/hero keep 'Click Start Test when you're ready'; once any results have been recorded the test is waiting on an external party, so the hero switches to 'Test running' / 'Waiting for an external request — no action required from you.' and the Start button is suppressed. Verified in-situ via Storybook (`WaitingHeroDistinguishesExternalVsUserAction` and `WaitingHeroKeepsStartWhenFreshTest`)." |
| closed | A1 | thomasdarimont, almgren | [#note_3371485017](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371485017) | `dc7e053a3` | "Fixed in `dc7e053a3` (U3). The status bar and hero now derive a lifecycle phase from `(status, result)` rather than from `status` alone, so a test whose verdict has landed but whose status field hasn't yet flipped to FINISHED renders the terminal branch immediately — no more Start button hanging around after 'Test has run to completion'. Verified on a real PASSED test (`zyH62JQ9NeDN0bD`): the bar shows PASSED + Repeat Test, no Start CTA. Story: `StaleStatusBarReadsTerminalWhenResultIsSet`." |
| closed | A2 | thomasdarimont | [#note_3371499052](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371499052) | `dc7e053a3` | "Fixed in `dc7e053a3` (U3). The header now renders a prominent terminal-state banner ('Test passed' on the pass palette, with the `circle-check` glyph) between the sticky bar and the hero whenever a test has reached a terminal phase. The verdict reads at a glance without scrolling to find the SUCCESS chip. Screenshot: `tmp/screenshots/u3-finished-pass-in-situ.png`." |
| closed | A3 | thomasdarimont | [#note_3371528678](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371528678) (A3 portion) | `0bd815594` | "The HTTP 400 portion is fixed in `0bd815594` (U4). Root cause: the v2 page-level handler in `log-detail.js` was sending the runtime test ID (e.g. `zyH62JQ9NeDN0bD`) as the `test=` query param, but `POST /api/runner` expects the *module name* (e.g. `oidcc-server`) — `TestRunner.java:215`. Continue Plan was 400'ing for a different reason in the same handler: it omitted `test=` entirely, expecting the backend to figure out 'next module' from `plan=` alone. The fix builds the URL via the same shape as `plan-detail.html`'s `buildRunnerUrl` (test=testName, &plan=, &variant=, Content-Type: application/json) and resolves the next module from the cached `/api/plan` modules list using subset-match on the variant constraints. Verified in-situ on a real OIDCC plan: Repeat POST returns HTTP 201, Continue POST returns HTTP 201 with the next module's testName. Error path now extracts the server's `{error:'…'}` body instead of showing 'HTTP 400'. This thread also contained C1 (label consistency, closed by `dc7e053a3`) — with A3 closed, the thread is fully addressed and can be resolved on the MR." |
| closed | A4 | thomasdarimont | [#note_3371494631](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371494631) (mixed with A5) | `5db5f2acb` | "Fixed in `5db5f2acb` — added a `Plan progress:` scope eyebrow before the `Module N of M` count in `cts-test-nav-controls`, so the bar now reads `Plan progress: Module 1 of 38` instead of just `Module 1 of 38`. The prefix scopes the count to the plan rather than the current test, addressing the misread you flagged. The bar's `aria-label` (`Test plan progress`) is unchanged. Verified in-situ on the live log-detail page (`zyH62JQ9NeDN0bD`, 38-module OIDCC plan): the eyebrow renders soft-bold ahead of the count and survives the slim-mode horizontal row layout. Screenshot: `tmp/screenshots/u5-plan-progress-eyebrow-viewport.png`. Stories: `MidPlan`, `SlimMidPlan`. With A5 also closed, the whole thread can now be resolved on the MR." |
| closed | A7 | almgren | [#note_3371772858](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371772858) | `dc7e053a3` | "Fixed in `dc7e053a3` (U3). The same terminal banner that closes A2 also applies to FAILED ('Test failed' on the fail palette, `close-circle` glyph) and INTERRUPTED ('Test interrupted'). Verified on a real INTERRUPTED+FAILED test (`VeJL6Fu5kmisBa3`): the banner sits above the existing interrupted alert + failure list, so 'did this test fail?' answers itself before any scrolling. Screenshot: `tmp/screenshots/u3-interrupted-in-situ.png`. Stories: `TerminalBannerFailed`, `TerminalBannerInterrupted`." |
| closed | B1 | thomasdarimont | TBD | `c2358951e` | "Fixed in `c2358951e` (U14). Added a tiny synchronous `<script>` at the top of `schedule-test.html`'s `<head>` that, when `?edit-plan=<id>` is present, immediately `window.location.replace`s to `plan-detail.html?plan=<id>` — fires before any module load or API call, so the page never paints the wrong surface during the redirect. Verified in-situ against the live dev server: `schedule-test.html?edit-plan=M1vPYjQWZWxKr` lands on `plan-detail.html?plan=M1vPYjQWZWxKr` rendering the real `authzen-pdp-test-plan` modules. The bare `schedule-test.html` still loads normally. e2e coverage: `frontend/e2e/schedule-test-url-compat.spec.js` (5 cases: happy path, no-redirect when absent, strips other params, encodes URL-unsafe chars, no-op on empty `?edit-plan=`). Heads-up: the in-app 'Edit configuration' callsites in `cts-plan-actions.js:487` and `log-detail.js:672` still emit the legacy URL shape, so they now bounce the user back to the same plan-detail page — those buttons need a separate decision (delete vs re-target to a new 'schedule new test from this config' URL); tracked as a follow-up, not in U14." |
| closed | B2 | almgren, joseph.heenan | [#note_3371602161](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371602161) (almgren: search/dropdown confusing), [#note_3373289866](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3373289866) (joseph.heenan: feels busy, prefers spec-selection-first) | `__U15SHA__` | "Fixed in `__U15SHA__` (U15), resolving G2. Rather than remove the search — which carries real discoverability value for new specs — the selector is redesigned into a two-column layout: the search field and a spec-family **listbox** (always-open, `size=15`) now sit in a compact left rail, with the plan list beside them on the right, instead of stacking search + a family *dropdown* + full-width list above the form. This keeps spec-family selection as the prominent first step (driving people through spec selection first, per Joseph) while making the block read less busy. The family control is now a real listbox: no dropdown chevron, long spec names wrap instead of clipping, and the active family reads in the design-system orange (`--orange-50`) rather than the browser's blue/grey highlight. Verified in-situ on the live schedule-test page — left rail with all 14 families visible, selecting *FAPI2 Security Profile* filters the right list, no clipping (`tmp/screenshots/u15-test-selector-listbox-in-situ.png`). Stories: `FamilyListboxStyleRegistered` + the `size=15` assertion in `Default`. @almgren @joseph.heenan — does this read less busy to you? Happy to iterate on rail width or ordering. Leaving both threads unresolved on the MR until you confirm, since this is a subjective UX call." |
| closed | B3 | almgren | [#note_3371602170](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371602170) | `f03b936ec` | "Fixed in `f03b936ec` (U11). The Load last configuration button now starts `loading disabled` and is gated by a single bootstrap probe to `/api/lastconfig` — if the response carries a `config` field the button enables; if the payload is the empty `{}` (`SavedConfigurationApi.java:32`) the button stays disabled, so the previously-mute click that 'seemingly does nothing but pops up an error dialog' is no longer reachable. Clicks on the enabled button toggle `cts-button`'s built-in `loading` spinner for the duration of the fetch (addresses the slow-reviewapp follow-up in #note_3371656812) and re-enable on either success or error so the user can retry. `cts-button.loading` and `cts-button.disabled` are now `reflect: true` so the host attribute mirrors the property — DOM observers, CSS, and e2e selectors all see the live state. Verified in-situ on the dev environment: with a real saved config the button enables after one probe call (`tmp/screenshots/u11-load-last-config-enabled.png`); with a mocked empty `{}` payload the button stays disabled with no spinner (`tmp/screenshots/u11-load-last-config-disabled.png`). Tests: `R13/U11: page load probes /api/lastconfig but does not apply it`, `U11: Load button starts disabled when no saved config exists`, `U11: Load button enables once probe reports a saved config`, `U11: clicking shows pending state and re-enables after success`, `U11: click error shows toast and re-enables the button`." |
| closed | B4 | almgren | [#note_3371656812](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371656812) | `c39b07032` | "Fixed in `c39b07032` (U12). Two paths were broken: (1) `loadLastConfigFromServer` in `schedule-test.html` triggered the cascade and stored `FAPI_UI.selectedVariant = data.variant`, but never propagated those values onto the rendered `.variant-selector` `<select>` elements. Because the variant dropdowns stayed on their `select` placeholder, `updateConfigFieldVisibility()` early-returned and `cts-config-form.schema` never bound — so the Form tab had zero fields to render even though `.config` was set. The new code mirrors `restoreEditPlanVariants`: walk `data.variant`, assign each value to its `<select>`, then re-run `updateVariantOptionsVisibility()` + `updateConfigFieldVisibility()` before `applyConfigToForm()`. (2) `cts-config-form`'s `_jsonText` state only refreshed on first JSON-tab activation or internal edits — external `.config` reassignment left a stale body in the Monaco editor for users who had already visited the JSON tab. Added a `willUpdate` hook that refreshes `_jsonText` when `config` changes externally; internal handlers update `config` and `_jsonText` in the same microtask, so the `!changedProperties.has(\"_jsonText\")` guard skips the refresh on internal paths (verified by `InternalJsonEditPreservesUserText`). Verified in-situ on a real saved OIDCC Basic config (`/api/lastconfig` → 6-key payload with `description`, `server.discoveryUrl`, `client.client_name`, `client2.client_name`, `browser[]`, `override`): variant dropdowns pre-select Discovery + Dynamic Client Registration, Form tab renders 9 fields with 4 populated to the saved values (description='oidc-provider OIDC', discoveryUrl, both client_names), and clicking the JSON tab shows the full pretty-printed body. Screenshots: `tmp/screenshots/u12-load-last-config-populated.png`, `tmp/screenshots/u12-form-tab-populated.png`. Stories: `ExternalConfigUpdatesFormFields`, `ExternalConfigUpdatesJsonEditor`, `InternalJsonEditPreservesUserText`." |
| closed | B5 | almgren | [#note_3371716830](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371716830) | `d3eb66198` | "Fixed in `d3eb66198` (U9). `cts-form-field` now routes string-typed fields to `<textarea>` when the dotted-path leaf matches one of `certificate` / `cert` / `jwks` / `key` / `pem` (with an optional `_chain` tail), so `client.certificate`, `mtls.cert`, `mtls.key`, `client.private_key`, and `vci.*_trust_anchor_pem` all paste cleanly across multiple lines. The matcher lives in `src/main/resources/static/lib/config-field-types.js` as a single regex anchored to the leaf segment — `server.jwks_uri` stays a single-line URL via both the `_uri` anchor and the existing `format: \"uri\"` short-circuit. The textareas also pick up `field-sizing: content` so they auto-grow as you type (capped at `max-height: 50vh`); Firefox lacks support today and falls back to the floor `min-height` plus the manual `resize: vertical` handle. Verified in-situ on a live `schedule-test.html` probe: the 7-case matrix routed correctly and an 11-line PEM paste grew the textarea from 96px to 224px. Screenshot: `tmp/screenshots/u9-textarea-in-situ.png`. Stories: `CertificateMultilineTextarea`, `PrivateKeyMultilineTextarea`, `MtlsCertMultilineTextarea`, `MtlsKeyMultilineTextarea`, `TrustAnchorPemMultilineTextarea`, `CertificateChainMatchesViaSuffix`, `JwksUriStaysSingleLineUrlInput`." |
| closed | B6 | almgren | [#note_3371722282](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371722282) | `2e514ca3d` | "Fixed in `2e514ca3d` (U10). The publish dropdown now renders three options — **No** / **Summary** / **Everything** — with **No** preselected, so the helper text `If in doubt choose \"No\"` once again points at a real choice rather than a blank entry. The wire format is unchanged: picking *No* submits `publish=\"\"`, which `TestRunner.java`'s `Strings.emptyToNull` already collapses to `null` (= unpublished). Implementation routes through the existing `cts-form-field` enum path: `config-field-catalog.json`'s `publish` entry now carries an `enumLabels: [\"No\", \"Summary\", \"Everything\"]` parallel array, `config-form-adapter.js#fieldToSchemaFragment` passes it through to the schema, and `cts-form-field._renderEnumOptions` uses it for display while keeping the wire `value` attribute on the original `\"\" / \"summary\" / \"everything\"` strings. As a side fix, when an enum already contains `\"\"`, the leading `<option value=\"\">Select...</option>` placeholder is suppressed so the dropdown no longer renders two `value=\"\"` options. Verified in-situ on a live `schedule-test.html` probe (OIDCC Basic plan, static/static_client variants): three options exactly, `No` preselected, change-to-Summary dispatches `cts-config-change` with `{publish:\"summary\"}` (wire value, not the display label). Screenshot: `tmp/screenshots/u10-publish-dropdown-zoom.png`. Stories: `PublishDropdownLabelsAndDefault`, `PublishDropdownChangeEmitsWireValue`." |
| closed | B7 | almgren | [#note_3371734137](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371734137) | `9c2fd4696` | "Fixed in `9c2fd4696` (U13). Clicking Validate Configuration now runs a synchronous schema pass and announces the verdict via `cts-toast` — `ok` toast when every visible required field is filled, `error` toast with a `N required field(s) are missing` count when it isn't. Every offending field also picks up the existing `.oidf-error` rust-border + inline 'Required field' callout, so the user sees both the global signal and the specific fix-up targets in one click. The construction-notice modal that previously short-circuited the button is gone. Required-field detection honours both schema conventions in the codebase: per-field `x-cts-required: true` (produced by `config-form-adapter` from `field.required` in `config-field-catalog.json`) and the section-level `required: []` arrays used by nested-mode fixtures; hidden fields are intentionally excluded because they are not user-actionable. A `cts-validate` event also fires with `{ valid, errors, config }` so a future backend hook (e.g. `POST /api/plan/validate` from brainstorm Unit 1D) can layer extra checks without changing the click handler. Verified in-situ on the live `openid-federation-deployed-entity-test-plan`: an empty form produced the error toast and a `.oidf-error` callout under `federation.entity_identifier`; typing a value and re-validating cleared the inline error and produced the success toast. Screenshots: `tmp/screenshots/u13-validate-success-toast.png`, `tmp/screenshots/u13-validate-error-viewport.png`, `tmp/screenshots/u13-validate-success-after-fix.png`. Stories: `ValidateButtonShowsSuccessToast`, `ValidateButtonShowsErrorToast`, `ValidateButtonIgnoresHiddenRequiredFields`." |
| closed | C1 | thomasdarimont | [#note_3371528678](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371528678) (label-consistency portion) | `dc7e053a3` | "Action label consistency landed in `dc7e053a3` (U3): the status-bar primaries now read 'Start Test' (WAITING) and 'Repeat Test' (FINISHED), matching cts-test-nav-controls' 'Repeat Test' / 'Continue Plan' so every action follows the `<Verb> <Object>` convention. The HTTP 400 portion of this thread (A3) ships in U4 — with that landed, the whole thread can be resolved on the MR." |
| partial | C2 (formatting) | thomasdarimont, almgren | TBD | `aafe7361f` | "The formatting half of C2 lands in `aafe7361f` (U6). `cts-test-summary` and `cts-log-detail-header`'s About-this-test hero + WAITING instructions hero now route the description through a new `formatDescription()` helper that splits on `\n\n` into `<p>` blocks and wraps backtick spans in `<code>`. The Variant row in the Test details drawer renders each key/value pair as a `<dt>`/`<dd>` line (the legacy `k: v, k: v` comma-soup is gone). Verified in-situ on a real PASSED test (`zyH62JQ9NeDN0bD`, 5-key variant): all five variant entries render on their own rows, and synthetic backtick/`\n\n` content produced the expected `<p>` + `<code>` DOM in the hero. Screenshot: `tmp/screenshots/u6-passed-test-formatted-description.png`. The rename half (About this test → Description, surface the one-line `description` as `Title:`) remains gated on G3 — please weigh in on G3 when convenient." |
| closed | D1 | thomasdarimont | [#note_3371552606](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371552606) | `706080a90` | "Fixed in `706080a90` (U7). Root cause: `/api/log/{testId}` serializes application payload at the entry's *top level* (`request_uri`, `response_body`, `expected`, `actual`, …); the Lit component was reading `entry.more`, which is always undefined on the wire — so the Details button short-circuited to `nothing` on every real row. Ported the legacy `LogEntryHelper.visibleFields` strip set to the frontend as `ENVELOPE_FIELDS`, added an `extractMoreFields()` helper that returns `entry.more` when present (preserves test-fixture shape + the `_formatCurl` flow) and otherwise synthesises the disclosure view by stripping envelope keys. Verified in-situ on the live OIDCC test `zyH62JQ9NeDN0bD` (121 entries on the wire, 117 cts-log-entry hosts): 92 Details buttons now render, expanding LOG-0003 reveals the 4 real-shape rows (`Request uri`, `Request method`, `Request headers`, `Request body`) with zero envelope leakage, matching the legacy baseline in screenshot 11. Stories: `ClickMoreRealHttpRequest`, `ClickMoreRealHttpResponse`, `EnvelopeOnlyEntryHasNoMoreButton`. Screenshot: `tmp/screenshots/u7-real-http-request-expanded.png`." |
| closed | D2 | thomasdarimont | [#note_3371566185](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371566185) | `85e29253e` | "Fixed in `85e29253e` (U8). Added `src/main/resources/static/lib/spec-links.js` — a one-fetch-per-page memoised loader for `api/ui/spec_links?public=true` mirroring the legacy `fapi.ui.js` bootstrap — and wired `cts-log-entry`'s requirement chips through `resolveSpecLink(ref, map)`, which does a longest-prefix match against the returned map. Mapped chips render as `<a target='_blank' rel='noopener noreferrer'>`; unmapped chips (e.g. typos like `OIDC-10.2.1`) degrade silently to `<span>` — no broken empty-href links. Verified in-situ on the live OIDCC log `zyH62JQ9NeDN0bD` (117 entries): 44 chips resolved to working anchors (`RFC7517-1.1` → `https://tools.ietf.org/html/rfc7517#section-1.1`, `OIDCC-10.1` → `https://openid.net/specs/openid-connect-core-1_0.html#rfc.section.10.1`), 1 typo'd chip stayed static — no broken links and zero new console errors. Screenshot: `tmp/screenshots/u8-spec-link-anchors-in-situ.png`. Stories: `SpecLinkResolvesMappedRefsToAnchors`, `SpecLinkUnknownRefStaysStatic`, `SpecLinkEmptyMapKeepsAllChipsStatic`, `SpecLinkLongestPrefixWins`." |
| gated | E1 (grid) | almgren | [#note_3371839485](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371839485) | — (G1 must resolve first) | — |
| closed | E1 (Config column) | almgren | [#note_3371839485](https://gitlab.com/openid/conformance-suite/-/merge_requests/1998#note_3371839485) | `07c43b26d` | "Fixed in `07c43b26d` (U16). Root cause was UI-only: the `/api/plan` response already carries each row's `config` field, but the new cellRenderer dropped the visible `label="View configuration"` that the legacy `templates/configButton.html` carried, so the column rendered a 34×16 ghost icon at the far right of the cell — visually empty next to the wide Modules column. The fix restores the `label="View configuration"` + `full-width` from the legacy template, and adds an empty-config guard: rows whose backend payload is `config: {}` (plans created without saved configuration — DBTestPlanService writes an empty `org.bson.Document`) no longer render a button that would open an empty modal. Verified in-situ on the live dev server: 7 rows on `plans.html`, 6 with non-empty configs each show a 30×155px full-width 'View configuration' CTA that opens a populated modal, and 1 row with `config: {}` (plan `M1vPYjQWZWxKr`) renders no Config affordance. Screenshots: `tmp/screenshots/u16-after-in-situ.png`, `tmp/screenshots/u16-modal-opens-with-config.png`. Stories: `Default` (asserts button count matches `MOCK_PLAN_LIST.filter(p => p.config && Object.keys(p.config).length > 0).length` and the visible label), `ConfigButtonHiddenWhenConfigIsEmpty` (single mocked empty-config row, asserts no `cts-button.showConfigBtn`). e2e: `plans.spec.js` 'loads and renders plans' asserts the same per-fixture button count + visible label. The per-module status grid half of this finding remains gated on G1; please weigh in on G1 when convenient." |

**Rows marked `TBD on next discussion fetch`:** I have not yet fetched every thread URL for every finding. Bootstrap rule from the skill: on the first execution invocation that touches one of these findings, fetch the corresponding thread via `glab api projects/openid%2Fconformance-suite/merge_requests/1998/discussions --paginate` and fill in the row before implementing.

---

## Decision Gate resolutions

Record maintainer decisions on G1 / G2 / G3 here as they land. Empty until the user (or a maintainer) answers.

| Gate | Question | Decision | Date | Decided by | Notes |
|---|---|---|---|---|---|
| G1 | `plans.html` per-module status grid — restore / aggregate / list-only | — | — | — | Blocks U17 |
| G2 | `schedule-test.html` "search test plans" header — remove / collapse / keep | **Keep, redesign as left rail** | 2026-05-26 | Kaelig (driving MR) | Fourth option beyond the enumerated three: keep the selector but restyle it — search + family **listbox** (`size=15`) in a left rail, plan list on the right. Compacts the block so it no longer pushes the SPECIFICATION cascade below the fold (Almgren's B2), while keeping spec-first selection prominent (Joseph's preference). Unblocks U15. |
| G3 | "About this test" labelling — rename to "Description" / "Title" or keep | — | — | — | Affects U6 follow-up (formatting ships regardless) |
| G4 | "Edit configuration" buttons on plan-detail / log-detail — delete or re-target after U14 redirect | — | — | — | Surfaced post-U14 (not in the original plan); blocks any cleanup of `cts-plan-actions.js:487` and `log-detail.js:672` |

### G4 — phrasing to send to Thomas

Posted as a follow-up reply on the same MR thread that surfaced the bookmark issue, written as a natural continuation rather than a tracker reference. Internal jargon (`U14`, `B1`, `G4`) deliberately stripped.

> One follow-up while we're on this: the **"Edit configuration"** button on the plan-detail page (and the matching action on log-detail) also links to `schedule-test.html?edit-plan=<id>` — same URL shape we just redirected. With the fix in place, those buttons now bounce you straight back to the page you were on.
>
> Two ways I can clean it up — which would you prefer?
>
> 1. **Drop the buttons.** The tooltip today reads *"Create a new test plan based on the configuration used in this one."* If that's not a flow people lean on during certification, just deleting it is the cleanest fix.
> 2. **Re-target them** to a new URL — something like `schedule-test.html?from-plan=<id>` — that loads the saved config into the create-test form. Preserves the workflow under a param that won't collide with the redirect.
>
> Either one is a small follow-up. Wanted to check before silently removing the affordance.

---

## How this file gets used

1. The `executing-plan-unit-by-unit` skill reads this file at the start of every unit-execution loop.
2. After verification passes and before `compound-engineering:ce-commit` fires, the unit's row(s) get `status` and `closing commit` filled.
3. The post-commit handoff to the user pulls the suggested-reply text from here.
4. The MR's discussion-level "Resolve" action is the user's call — the agent does not toggle thread resolution on the MR itself.
