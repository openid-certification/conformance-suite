apiVersion: v2
description: Helm chart for FAPI conformance suite
name: fapi-conformance-suite
version: 0.1.1
dependencies:
    - name: mongodb
      # this is currently the latest release from https://artifacthub.io/packages/helm/bitnami/mongodb/ that uses mongo 5.0.x
      version: "12.1.31"
      repository: "https://charts.bitnami.com/bitnami"
