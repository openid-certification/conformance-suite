version: '3'
services:
  mongodb:
    image: mongo
    volumes:
     - ./mongo/data:/data/db
  httpd:
    build:
      context: ./httpd
    ports:
     - "8443:8443"
    depends_on:
     - server
  server:
    image: openjdk:9
    volumes:
     - ./target/:/server/
    command: java -jar /server/fapi-test-suite.jar --fintechlabs.devmode=true
    links:
     - mongodb:mongodb
    depends_on: 
     - mongodb
    

