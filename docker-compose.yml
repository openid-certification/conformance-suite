version: '3'
services:
  mongodb:
    image: mongo
    volumes:
     - ./mongo/data:/data/db
  httpd:
    build:
      context: ./httpd
    ports:
     - "8080:8080"
    depends_on:
     - server
  server:
    image: openjdk
    volumes:
     - ./target/:/server/
    command: java -jar /server/fapi-test-suite.jar
    links:
     - mongodb:mongodb
    depends_on: 
     - mongodb
    

